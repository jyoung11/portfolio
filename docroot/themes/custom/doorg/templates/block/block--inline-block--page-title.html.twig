{#
/**
 * @file
 * Custom template for the Page Title block.
 *
 * Available variables:
 * - content: The content of the block.
 * - label: The block label.
 * - configuration: A list of the block's configuration values.
 * - plugin_id: The ID of the block plugin.
 * - base_plugin_id: The base ID of the block plugin.
 * - derivative_plugin_id: The derivative ID of the block plugin.
 * - attributes: HTML attributes for the block.
 * - title_white: The white title field of the block.
 * - title_orange: The orange title field of the block.
 */
#}
<div{{ attributes.addClass('block', 'block--page-title') }}>
{{ title_suffix.contextual_links }}
  {% if label %}
    <h2>{{ label }}</h2>
  {% endif %}
  {% set tag = node.field_hero_title_white.0.value is not empty ? 'h2' : 'h1' %}
  <{{ tag }}>
    {{ page_title_white }}
    {% if page_title_orange is not empty %}
      <span class="orange">{{ page_title_orange }}</span>
    {% endif %}
  </{{ tag }}>
</div>